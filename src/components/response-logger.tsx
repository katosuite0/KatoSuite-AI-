"use client";

import { useEffect } from "react";

export const ResponseLogger: React.FC = () => {
  useEffect(() => {
    // Placeholder hook for environments where responses are tracked.
  }, []);

  return null;
};

export default ResponseLogger;
